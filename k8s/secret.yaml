apiVersion: v1
kind: Secret
metadata:
  name: fruits-api-secret
  labels:
    app: fruits-api
type: Opaque
stringData:
  # Demo credentials only. Replace before any real deployment, e.g.
  # kubectl create secret generic fruits-api-secret \
  #   --from-literal=DB_USER=postgres --from-literal=DB_PASSWORD='<strong-password>'
  DB_USER: postgres
  DB_PASSWORD: postgres

basePath: /
definitions:
  models.CreateFruitRequest:
    properties:
      color:
        example: yellow
        type: string
      fruit:
        example: banana
        type: string
    required:
    - color
    - fruit
    type: object
  models.ErrorResponse:
    properties:
      error:
        example: fruit not found
        type: string
    type: object
  models.Fruit:
    properties:
      color:
        example: red
        type: string
      fruit:
        example: apple
        type: string
      id:
        example: 1
        type: integer
    type: object
  models.HealthResponse:
    properties:
      status:
        example: ok
        type: string
    type: object
host: localhost:8080
info:
  contact: {}
  description: RESTful API for managing fruits, backed by PostgreSQL.
  title: Fruits API
  version: "1.0"
paths:
  /fruits:
    get:
      description: Returns every fruit in the database, or an empty array when there
        are none.
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            items:
              $ref: '#/definitions/models.Fruit'
            type: array
        "500":
          description: Internal Server Error
          schema:
            $ref: '#/definitions/models.ErrorResponse'
      summary: List all fruits
      tags:
      - fruits
    post:
      consumes:
      - application/json
      description: Adds a fruit and returns it with the generated ID.
      parameters:
      - description: Fruit to create
        in: body
        name: fruit
        required: true
        schema:
          $ref: '#/definitions/models.CreateFruitRequest'
      produces:
      - application/json
      responses:
        "201":
          description: Created
          schema:
            $ref: '#/definitions/models.Fruit'
        "400":
          description: Bad Request
          schema:
            $ref: '#/definitions/models.ErrorResponse'
        "500":
          description: Internal Server Error
          schema:
            $ref: '#/definitions/models.ErrorResponse'
      summary: Create a fruit
      tags:
      - fruits
  /fruits/{id}:
    get:
      description: Returns a single fruit.
      parameters:
      - description: Fruit ID
        in: path
        name: id
        required: true
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/models.Fruit'
        "400":
          description: Bad Request
          schema:
            $ref: '#/definitions/models.ErrorResponse'
        "404":
          description: Not Found
          schema:
            $ref: '#/definitions/models.ErrorResponse'
        "500":
          description: Internal Server Error
          schema:
            $ref: '#/definitions/models.ErrorResponse'
      summary: Get a fruit by ID
      tags:
      - fruits
  /healthz:
    get:
      description: Reports service readiness, including database connectivity.
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/models.HealthResponse'
        "503":
          description: Service Unavailable
          schema:
            $ref: '#/definitions/models.ErrorResponse'
      summary: Health check
      tags:
      - operations
schemes:
- http
- https
swagger: "2.0"
